#include<cstdio>
#include<cstring>
#include<iostream>
#include<string>
#include<algorithm>
#define maxn 10010
#define ll long long
using namespace std;
inline int read(){
	int a=0,b=0;
	char ch=getchar();
	while(!isdigit(ch))b^=ch=='-',ch=getchar();
	while(isdigit(ch))a=a*10+(ch-'0'),ch=getchar();
	return b?-a:a;
}
int main(){
	
	return 0;
}
